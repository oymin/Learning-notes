## Consumer

> 表示接受单个输入参数且不返回结果的操作

```
@FunctionalInterface
public interface Consumer<T> {
}
```

> 函数式方法：void accept(T t);

```
# 接收一个参数，不返回值

void accept(T t);
```

http://f28de43d.ngrok.io/po/login
